<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    {# This CSS file should contain your styles for .hero-background, .card-glass, etc. #}
    {# <link rel="stylesheet" href="{{ BASE_PATH }}/css/viewitem.css"> #}
    <style>
        /* These styles should be in viewitem.css, but are here for completeness */
        body {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            margin: 0;
            background-color: #1B222C; /* Added dark background as fallback */
        }
        main {
            flex-grow: 1;
        }
        .hero-background {
            position: relative;
            background-size: cover;
            background-position: center;
            min-height: 100vh; 
            display: flex; 
            align-items: center; 
            padding-top: 80px; /* Extra padding for navbar */
            padding-bottom: 56px; 
        }
        .hero-background::before {
            content: '';
            position: absolute;
            top: 0; left: 0; right: 0; bottom: 0;
            background-color: rgba(0, 0, 0, 0.55);
            z-index: 1;
        }
        .hero-content {
            position: relative;
            z-index: 2;
            width: 100%; 
        }
        .card-glass {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
            border-radius: 1rem;
            border: 1px solid rgba(255, 255, 255, 0.2);
            color: #E2E8F0; /* Ensure text is light */
        }
        .card-glass hr { border-top: 1px solid rgba(255, 255, 255, 0.3); }
        .card-glass .fs-4, .card-glass .fs-5, .card-glass .fs-6, .card-glass .small {
             color: #E2E8F0 !important; /* Ensure all text is light */
        }
        .card-glass .text-muted {
             color: #A0AABA !important; /* Lighter muted color */
        }
         @media (max-width: 767.98px) {
            .hero-img {
                 max-height: 250px;
                 width: auto;
             }
         }
         
         /* Styles for Modals in Dark Mode */
        .modal-content {
            background-color: #2A3644; /* var(--card-bg) */
            border: 1px solid rgba(255, 255, 255, 0.1); /* var(--input-border-color) */
            color: #E2E8F0; /* var(--text-color) */
        }
        .modal-header {
             border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }
        .modal-footer {
             border-top: 1px solid rgba(255, 255, 255, 0.1);
        }
        .btn-close-white {
            filter: invert(1) grayscale(100%) brightness(200%);
        }
        /* Dark form controls for modal */
        .form-control-dark {
            background-color: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.1);
            color: #fff !important;
        }
        .form-control-dark::placeholder {
            color: #6c757d;
        }
        .form-control-dark:focus {
            background-color: rgba(255, 255, 255, 0.05);
            border-color: #007bff;
            box-shadow: none;
            color: #fff !important;
        }
    </style>
    <title>{{ item.item_name | default('Item Details') }}</title>
</head>

<body>
    <header>
        <nav class="navbar fixed-top" data-bs-theme="dark">
            <div class="container-fluid">
                <a class="navbar-brand px-3 py-2" href="{{ BASE_PATH }}/lostitems"> 
                    <i class="bi bi-arrow-left-square fs-3"></i>
                </a>
            </div>
        </nav>
    </header>

    <main>
        <div class="container-fluid p-0 hero-background" style="background-image: url('{{ BASE_PATH }}/{{ item.item_image_url | default('https://placehold.co/1920x1080/6c757d/FFFFFF?text=No+Background+Image') }}');">

            <div class="container hero-content">

                {# --- ALERTS for Claim Submission --- #}
                {% if error_message is defined and error_message %}
                    <div class="alert alert-danger alert-dismissible fade show" role="alert">
                        <strong>Claim Failed!</strong> {{ error_message }}
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                {% endif %}
                {% if success_message is defined and success_message %}
                    <div class="alert alert-success alert-dismissible fade show" role="alert">
                        <strong>Success!</strong> {{ success_message }}
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                {% endif %}
                {# --- END ALERTS --- #}

                <div class="row align-items-center text-white"> 
                    
                    <div class="col-md-6 d-flex justify-content-center mb-4 mb-md-0 text-center">
                        <img src="{{ BASE_PATH }}/{{ item.item_image_url | default('https://placehold.co/600x600/6c757d/FFFFFF?text=No+Item+Image') }}"
                             class="img-fluid rounded-3 shadow-lg hero-img"
                             style="object-fit: contain; max-height: 450px;" 
                             alt="{{ item.item_name }}">
                    </div>
                    
                    <div class="col-md-6 text-white text-center text-md-start">
                        
                        <h1 class="display-5 display-md-3 fw-bold">{{ item.item_name }}</h1>
                        <p class="fs-4 fs-md-3 text-white">{{ item.category_name | default('Uncategorized') }}</p>

                        <div class="card-glass p-3 p-md-4 mt-3">
                            <div class="row">
                                <div class="col-12 col-sm"><p class="fs-5 fs-md-4 mb-2">Description</p></div>
                                <div class="col-12 col-sm">
                                    <p class="fs-5 fs-md-4 mb-2 text-sm-end"> 
                                        {% if item.item_status == 'found' %}<i class="bi bi-check-circle-fill text-success pe-2"></i>Available (Found)
                                        {% elseif item.item_status == 'claimed' %}<i class="bi bi-person-check-fill text-primary pe-2"></i>Claimed
                                        {% elseif item.item_status == 'archived' %}<i class="bi bi-archive-fill text-secondary pe-2"></i>Archived
                                        {% else %}<i class="bi bi-question-circle-fill text-warning pe-2"></i>Unknown
                                        {% endif %}
                                    </p>
                                </div>
                            </div>
                            <hr class="mt-0 mb-2">
                        
                            <p class="small">
                                {{ item.item_description | default('No description provided.') }}
                            </p>
                            <hr class="mt-2 mb-2">
                            <div class="row">
                                <div class="col-6 text-center">
                                    <p class="fs-6 fs-md-5 m-0"><i class="bi bi-geo-alt-fill pe-1"></i>Location</p>
                                    <p class="small">{{ item.found_location | default('N/A') }}</p>
                                </div>
                                <div class="col-6 text-center">
                                    <p class="fs-6 fs-md-5 m-0"><i class="bi bi-person-fill pe-1"></i>Reporter</p>
                                    <p class="small">{{ item.reporter_first_name | default('') }} {{ item.reporter_last_name | default('') }}</p>
                                </div>
                                <hr class="mt-2 mb-1">
                                <div class="text-center">
                                    <p class="small">Reported: {{ item.reported_at | date("M j, Y g:i a") }}</p>
                                </div>
                            </div>
                            
                            {# --- CLAIM BUTTON --- #}
                            {# Only show this button if the item status is 'found' #}
                            {% if item.item_status == 'found' %}
                            <div class="d-grid mt-4">
                                <button class="btn btn-lg btn-success" data-bs-toggle="modal" data-bs-target="#claimModal">
                                    <i class="bi bi-hand-thumbs-up-fill me-2"></i> Request to Claim This Item
                                </button>
                            </div>
                            {% endif %}
                            {# --- END CLAIM BUTTON --- #}

                        </div>
                    </div>
                </div>

            </div>

        </div>
    </main>
    
    {# This template seems to be missing a footer block, but it should be fine if it's not extending a base. #}

<!-- ============================================== -->
<!-- == NEW: Claim Item Modal == -->
<!-- ============================================== -->
<div class="modal fade" id="claimModal" tabindex="-1" aria-labelledby="claimModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="claimModalLabel">Submit Your Claim for: {{ item.item_name }}</h5>
        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <form action="{{ BASE_PATH }}/submit_claim" method="POST">
          <div class="modal-body">
            <p class="text-muted small">To claim this item, please provide your details for verification. An admin will review your request.</p>
            
            {# Hidden field to send the item ID #}
            <input type="hidden" name="item_id" value="{{ item.id }}">

            <div class="row">
                <div class="col-md-6 mb-3">
                    <label for="claim_first_name" class="form-label">First Name</label>
                    <input type="text" class="form-control form-control-dark" id="claim_first_name" name="first_name" required>
                </div>
                <div class="col-md-6 mb-3">
                    <label for="claim_last_name" class="form-label">Last Name</label>
                    <input type="text" class="form-control form-control-dark" id="claim_last_name" name="last_name" required>
                </div>
            </div>
            <div class="mb-3">
                <label for="claim_student_id" class="form-label">Student ID</label>
                <input type="text" class="form-control form-control-dark" id="claim_student_id" name="student_id" required>
            </div>
             <div class="mb-3">
                <label for="claim_email" class="form-label">Email Address</label>
                <input type="email" class="form-control form-control-dark" id="claim_email" name="email" required>
            </div>
             <div class="mb-3">
                <label for="claim_contact_number" class="form-label">Contact Number</label>
                <input type="tel" class="form-control form-control-dark" id="claim_contact_number" name="contact_number" required>
            </div>
            <div class="mb-3">
                <label for="claim_proof" class="form-label">Proof of Ownership (Optional but helpful)</label>
                <textarea class="form-control form-control-dark" id="claim_proof" name="proof_description" rows="2" placeholder="e.g., 'My phone has a blue case with a sticker on it', 'My wallet contains a specific ID card'"></textarea>
            </div>

          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
            <button type="submit" class="btn btn-primary">Submit Claim</button>
          </div>
      </form>
    </div>
  </div>
</div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
